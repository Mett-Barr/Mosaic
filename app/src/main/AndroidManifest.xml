<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      enableOnBackInvokedCallback is predictive back, and is ignored below API
      33. tools:targetApi records that the attribute is deliberate rather than
      left over from a higher minSdk. Lint cannot tell those apart on its own,
      and without it the only signal is a warning nobody should act on.
    -->
    <application
        android:name=".MosaicApp"
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="33"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mosaic">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Mosaic">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

# AI 使用

## 工具與分工

| | |
|---|---|
| **Claude Code**（Opus 5） | 產出：寫測試、寫實作、跑 gate、commit |
| **Codex**（`gpt-5.6-luna`，reasoning effort `max`） | 獨立審查：在人審之前讀 diff，任務是**提出反對意見** |
| **人** | 定義行為與驗收條件、審測試、審 diff、架構取捨 |

用**兩個不同實驗室**的模型而不是同一個模型跑兩輪，是因為同一個模型的第二意見會複製
它自己的盲區。八輪審查裡有兩次它抓到的 bug 是我自己複製自己的錯誤造成的——換成
「再問 Claude 一次」不會抓到。

Codex 的意見是**建議而非閘門**：它擋不住 commit，人可以否決。審查也不進 commit
trailer——**審查不構成作者身分**，trailer（`Assisted-by: LLM Claude Code`）只記誰產出。

## 代表性的幾次提問

- 「這是六個 must-have 的規格、目前的 diff 與模組結構。**你的任務是找出反對意見**，
  不是核可。每一條說：哪裡錯、為什麼要緊、你會怎麼改，並標明 blocking／should-fix／意見。」
- 「`published_at_lte` 這個做法，**是不是我誤讀了 API**？客戶端時鐘對上伺服器端的過濾器，
  時鐘偏差會壞到什麼程度？」
- 「快取用裝飾器、天氣用建構子參數——**這個不對稱是真的區別還是我在合理化不一致**？」

## 接受了什麼、否決了什麼

| 它說的 | 我怎麼做 |
|---|---|
| **天氣卡會消失**：`loaded()` 主要路徑沒帶 `weather`，天氣先回來或載入下一頁後卡就不見 | **原封不動接受。**真的是 bug，打中的正是第六個 must-have |
| **`Instant.parse` 丟的是 `DateTimeParseException`，不是 `IllegalArgumentException`** ——三個檔案存放器的防護全部漏接 | **原封不動接受。**六個測試寫下去全紅 |
| `writeText` 先清空再寫，程序在中間被殺會清掉讀者的閱讀清單 | **接受。**三個存放器改成寫暫存檔再 rename |
| `DECISIONS.md` 把 `published_at_lte` 的保證**講得太滿**——那是時間截點不是 snapshot | **接受這個更正。**改寫成「大幅降低而非消除」，並列出擋不住的四種情況 |
| 我對「未來時間戳」的理由（時區、換日線）**不成立** | **接受更正，結論不變。**那些不改變 `Instant`，真正成因是使用者改時間 |
| 快取用裝飾器、天氣用建構子參數的不對稱，**理由站不住腳** | **接受批評，維持現狀。**它自己也說在這個尺度是合理簡化。理由改寫，不再宣稱那是原則問題 |
| 引入 Robolectric 才能測畫面 | **否決。**新相依 + 時間不夠。代價寫進 README 的已知限制，不假裝沒有 |
| 建議把分頁改成整份 snapshot 快取 | **否決。**快取變大、寫入變頻繁、freshness 語意要重想，收益不成比例 |

## 一個 AI 弄錯而被抓到的地方

分頁的 offset 會位移（新文章從頂端插入），我從**第一原理**推導出兩個解法：用 id 去重、
或不要快取游標。兩個都是繞路——前者只治重複不治遺漏，後者等於放棄分頁。

**是人打斷的**：「這邊應該是看它的 API 怎麼設計，然後再怎麼去接吧。你的講法看起來
怎麼好像把整個 paging 放棄掉。」

去查了 API，`published_at_lte` 一直都在，而且伺服器會把它寫進自己產生的每一個 `next`
連結。**先讀 API 再設計，不要先設計再遷就 API。** 這一條記在 `DECISIONS.md` 16。

## 另外兩件值得分享的

**假物件的時序一致，等於沒有 race。** 天氣卡那個 bug 之所以測試全綠，是因為兩個假
repository 都只 `yield()` 一次，「文章先完成」被固定下來，race 從來沒真的發生過。
**兩個來源用同一種時序，等於只有一個來源。**

**agent 自述完成不算證據。** 唯一的完成定義是本地實跑 `./gradlew build detekt lint` 全綠——
這是 [`AGENTS.md`](AGENTS.md) 寫死的規則。另外有兩件事是**測試問不出來、跑起來一眼就看到**的：
離線重開後天氣卡消失、日期顯示成 `31 8月, 21:14`。

---

逐輪的完整紀錄（八次審查、每一條的處置與理由）在
[`docs/ai-review-log.md`](docs/ai-review-log.md)。

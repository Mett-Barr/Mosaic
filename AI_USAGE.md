# AI 使用

## 工具與分工

| | |
|---|---|
| **Claude Code**（Opus 5） | 產出：寫測試、寫實作、跑 gate、commit |
| **Codex**（`gpt-5.6-luna`，reasoning effort `max`） | 獨立審查：在人審之前讀 diff，任務是**提出反對意見** |
| **人** | 定義行為與驗收條件、審測試、審 diff、架構取捨 |

用**兩個不同實驗室**的模型而不是同一個模型跑兩輪，是因為同一個模型的第二意見會複製
它自己的盲區。八輪審查裡有兩次它抓到的 bug 是我自己複製自己的錯誤造成的——換成
「再問 Claude 一次」不會抓到。

Codex 的意見是**建議而非閘門**：它擋不住 commit，人可以否決。審查也不進 commit
trailer——**審查不構成作者身分**，trailer（`Assisted-by: LLM Claude Code`）只記誰產出。

## 代表性的幾次提問

- 「這是六個 must-have 的規格、目前的 diff 與模組結構。**你的任務是找出反對意見**，
  不是核可。每一條說：哪裡錯、為什麼要緊、你會怎麼改，並標明 blocking／should-fix／意見。」
- 「`published_at_lte` 這個做法，**是不是我誤讀了 API**？客戶端時鐘對上伺服器端的過濾器，
  時鐘偏差會壞到什麼程度？」
- 「快取用裝飾器、天氣用建構子參數——**這個不對稱是真的區別還是我在合理化不一致**？」

## 接受了什麼、否決了什麼

| 它說的 | 我怎麼做 |
|---|---|
| **天氣卡會消失**：`loaded()` 主要路徑沒帶 `weather`，天氣先回來或載入下一頁後卡就不見 | **原封不動接受。**真的是 bug，打中的正是第六個 must-have |
| **`Instant.parse` 丟的是 `DateTimeParseException`，不是 `IllegalArgumentException`** ——三個檔案存放器的防護全部漏接 | **原封不動接受。**六個測試寫下去全紅 |
| `writeText` 先清空再寫，程序在中間被殺會清掉讀者的閱讀清單 | **接受。**三個存放器改成寫暫存檔再 rename |
| `DECISIONS.md` 把 `published_at_lte` 的保證**講得太滿**——那是時間截點不是 snapshot | **接受這個更正。**改寫成「大幅降低而非消除」，並列出擋不住的四種情況 |
| 我對「未來時間戳」的理由（時區、換日線）**不成立** | **接受更正，結論不變。**那些不改變 `Instant`，真正成因是使用者改時間 |
| 快取用裝飾器、天氣用建構子參數的不對稱，**理由站不住腳** | **接受批評，維持現狀。**它自己也說在這個尺度是合理簡化。理由改寫，不再宣稱那是原則問題 |
| **「立刻」那一句對任何實作都是真的**：`assertEquals(0L, currentTime)` 寫在 `runCurrent()` 之後，而 `runCurrent()` 不推進虛擬時鐘 | **原封不動接受。**那一輪的工作**就是**在找不會失敗的斷言，找到兩個，走過第三個沒看見——而它在我剛改過的五行之內。刪掉，語意併進上一句的計數斷言 |
| 引入 Robolectric 才能測畫面 | **否決。**新相依 + 時間不夠。代價寫進 README 的已知限制，不假裝沒有 |
| 建議把分頁改成整份 snapshot 快取 | **否決。**快取變大、寫入變頻繁、freshness 語意要重想，收益不成比例 |

## 一個 AI 弄錯而被人抓到的地方

分頁 offset 會位移，我從第一原理推導出兩個解法，兩個都是繞路。**是人打斷的**：
「這邊應該是看它的 API 怎麼設計吧，你的講法好像把整個 paging 放棄掉。」
去查了 API，`published_at_lte` 一直都在。**先讀 API 再設計，不要先設計再遷就 API**
（`DECISIONS.md` 16）。

## 我弄錯而 AI 抓到的，比反過來多

後期我把任務拆給 subagent。**三次我在任務書裡寫錯 API，而它們查了文件、沒有照做**：
共享元素的尺寸修飾符該放在共享修飾符**之後**（我寫成之前，照做會弄壞本來對的那端）；
「Compose 沒有形狀動畫」——文件說的是沒有**自動**形狀動畫，`OverlayClip` 是介面，
每一幀拿得到當下邊界；`NavDisplay` 放進 `Scaffold` 時官方 recipe **丟棄** inner padding，
我們多傳了它，容器變形的目標因此變成「螢幕減掉 bar」，展開明顯分兩段。

第二條是我自己寫進 `DECISIONS.md` 33 的結論，後來由 35 推翻，**而 33 沒有被改寫**。

最後一輪讓 Claude 與 Codex 各自獨立審同一份 diff，它們在兩點上**直接衝突**。爭點之一：
統一兩條彈簧的勁度，等不等於兩條曲線同步？Codex 說不等於（型別、位移、閾值都不同），
Claude 說等於（臨界阻尼、同 ω，正規化軌跡 `x(t)/x₀ = (1+ωt)e^{-ωt}` 與位移無關）。

**Claude 對，而我跟著 Codex 算了一次也錯。** 我算的是「各自何時收斂」，該看的是
「每一幀走完旅程的幾分之幾」——只有後者決定容器變形看起來對不對。

贏的那方兩次都**去讀了原始碼**，輸的兩次都**從 API 名稱推理**。這是整個專案最一致的
一條規律，也改變了我寫任務書的方式：把不確定的地方標成「這是推測，請驗證」，
比假裝確定有用得多。兩邊獨立指向同一批問題的部分可信度最高，全部接受。

## 工具本身也會騙人

Codex 的獨立審查失敗過五次，五種形態，**其中三次回報 exit 0**：輸出寫在 repo 內被自己的
`rg` 掃到形成回授迴圈；輸出移出後改為無邊界地讀進函式庫原始碼與 build 中間檔；prompt 當
命令列參數傳而 `Argument list too long`，且 wrapper 把錯誤印成 `node: No error`。第六次成功。

它抓到兩個 Claude 這邊漏掉的：一個 `graphicsLayer` 的失效漏洞，以及**一個被文件記錄成
保護、實際上是空操作的 `coerceIn`**——時鐘往後跳三天，那個「保護」讓 app 三天不發任何請求。
諷刺的是同一輪的另一個 commit 修的正是它的鏡像：那一輪把同一根釘子往下移了一個分支，
然後寫了一條決策說它移除了。**文件比程式碼先進一步**，正是這個專案最在意的失敗模式。

我**否決**了它兩條：`NavKey` 換 package 使序列化名稱改變（技術上對，但這個 app 從未發佈，
Android 的 saved state 也不跨安裝存活）；以及「縮圖圓角違反不改動靜止畫面的約束」——
那正是我要它修的那一項，Codex 沒有任務書的上下文。

## 另外三件值得分享的

**假物件的時序一致，等於沒有 race。** 天氣卡那個 bug 之所以測試全綠，是因為兩個假
repository 都只 `yield()` 一次，「文章先完成」被固定下來，race 從來沒真的發生過。

**寫得出斷言不等於測得到行為。** `AGENTS.md` 審測試四點的第四點是「把實作整段移除，
它還會失敗嗎」。把 bug 注回去實測：對原本的測試 11 tests 0 failed，對修正後的測試 1 failed。
同樣手法發現另一組測試把一個有整段理由的常數的邊界留在 `[2, 12]` 之間。

**單張截圖不是動畫的驗證。** 驗收標準換過三次，每次都因為前一個漏掉東西：截圖漏掉一切
會隨時間改變的東西；放慢十倍逐幀漏掉「兩條曲線速率不同」；最後是讀原始碼算數學。
逐幀比對要**用矩形高度而不是幀號對齊**——模擬器每秒只畫得出約 11 幀。

---

逐輪的完整紀錄在 [`docs/ai-review-log.md`](docs/ai-review-log.md)。
